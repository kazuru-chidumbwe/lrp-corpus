# Profiles

Profile bundles will appear here as they are released.

Each subdirectory is one LRP profile:

```
profiles/<profile_id>/
  manifest.json    # validates against schema/lrp-profile-v1.schema.json
  dg1.bin
  dg2.bin
  sod.bin
  ...
```

This directory is intentionally empty except for this README.
